
Make local tomcat run on 9000

Add -Dcom.pyx4j.appConfig=DevTomcat

Create mail-credentials.properties  in tomcat home.

--- Custom configuration on Tomcat

Add -Dcom.pyx4j.appConfig=DevCustom

  Place the file config.properties.template  to catalina.base/conf/${vista.war.name}/config.properties

-------
    Q: When uploading a WAR file larger than 50 MB,
     Get error: the request was rejected because its size (XXX) exceeds the configured maximum (52428800)

    Solution:
        Go to the web.xml of the manager application (for instance it could be under /tomcat7/webapps/manager/WEB-INF/web.xml
        Increase the max-file-size and max-request-size


---
 Test deployment to local tomcat:

  Add to .m2/settings.xml,  Don't forget to add role 'manager-script' in tomcat-users.xml to this user
                <server>
                    <id>tomcat.localhost</id>
                    <username>admin</username>
                    <password>123456</password>
                </server>

  mvn -Dmaven.test.skip=true -P deploy,deploy-target-local,deploy-db-reset

Profiling deploymnet:
  mvn -Dmaven.test.skip=true -P deploy,deploy-target-www11,deploy-db-reset-mini

---
 JMX to dev server  (http://blog.markfeeney.com/2010/10/jmx-through-ssh-tunnel.html)
 Tunel 9102 and 9103 ports


--
To configure Tomcat to support remote debugging?
  Add this to potions (ON A SEPARATE LINEs)
  -Xdebug
  -Xrunjdwp:transport=dt_socket,address=8000,server=y,suspend=n


--
 Build sever 'dev' deployment memory MemoryLeakPrevention
 Even so we have JreMemoryLeakPreventionListener.driverManagerProtection http://tomcat.apache.org/tomcat-7.0-doc/config/listeners.html#JRE_Memory_Leak_Prevention_Listener_-_org.apache.catalina.core.JreMemoryLeakPreventionListener
 On build server I removed the META-INF/services from
   /.m2/repository/postgresql/postgresql/9.1-901-1.jdbc4/ postgresql-9.1-901-1.jdbc4.jar
   /.m2/repository/mysql/mysql-connector-java/5.1.20/ mysql-connector-java-5.1.20.jar